# 07.5 Stream Interview Questions

These questions cover the most important Stream API concepts frequently discussed in Java interviews.

If you can confidently answer these questions, you have a strong understanding of the Stream programming model.

---

# Stream Fundamentals

### 1. Why was the Stream API introduced?

What problems does it solve compared to traditional loops?

---

### 2. What is the difference between a Stream and a Collection?

Explain how they differ in purpose, storage, and iteration.

---

### 3. What is the difference between External Iteration and Internal Iteration?

Why is Internal Iteration considered one of the biggest advantages of the Stream API?

---

### 4. Explain the Stream Pipeline.

Describe the role of:

* Source
* Intermediate Operations
* Terminal Operation

---

### 5. What is Lazy Evaluation?

Why are Intermediate Operations called lazy?

---

### 6. Why can a Stream be consumed only once?

What happens if you try to reuse it?

---

# Stream Operations

### 7. What is the difference between Intermediate and Terminal Operations?

Give examples of each.

---

### 8. What is the difference between `filter()` and `map()`?

When would you use each?

---

### 9. What is the difference between `map()` and `flatMap()`?

Explain using a nested collection example.

---

### 10. What is the purpose of `peek()`?

Why is it primarily used for debugging?

---

### 11. What is the difference between `findFirst()` and `findAny()`?

When is `findAny()` more beneficial?

---

### 12. What is the difference between:

* `anyMatch()`
* `allMatch()`
* `noneMatch()`

---

### 13. What is the purpose of the `reduce()` operation?

Give a real-world use case.

---

# Collectors

### 14. What is the difference between `collect()` and `Collectors`?

---

### 15. What is the difference between:

* `toList()`
* `Collectors.toList()`

---

### 16. When would you use `groupingBy()` instead of `partitioningBy()`?

---

### 17. What problem does `mapping()` solve inside `groupingBy()`?

---

### 18. What happens if duplicate keys are encountered while using `Collectors.toMap()`?

How can you handle this situation?

---

### 19. Why would you use `summarizingInt()` instead of performing multiple Stream operations?

---

# Scenario-Based Questions

### 20. You have a list of employees.

How would you use the Stream API to:

* Filter active employees.
* Convert names to uppercase.
* Group employees by department.
* Calculate salary statistics.
* Find the top three highest-paid employees.

Which Stream operations and Collectors would you choose, and why?

---

### 21. When would you choose a traditional loop instead of the Stream API?

Can you think of situations where a loop is a better choice?

---

### 22. What are the most common mistakes developers make while using the Stream API?

How can they be avoided?

---

## Navigation

⬅️ **[Previous: Stream Coding Problems](./07.4%20Stream%20Coding%20Problems.md)**

🏠 **[Home: Java 8 Interview Handbook](../../Java%208%20Interview%20Handbook.md)**

➡️ **[Next: Optional](../08.%20Optional.md)**
